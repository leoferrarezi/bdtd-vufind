<?php
  // BDTD: lista simples (sem links) para Instituição, Programa, Departamento e País,
  // como no legado. Aceita strings ou objetos PublicationDetails (convertidos para texto).
  $values = array_filter(array_map(fn ($v) => trim((string)$v), (array)$data), 'strlen');
?>
<?=implode('<br>', array_map([$this, 'escapeHtml'], array_values(array_unique($values))))?>
